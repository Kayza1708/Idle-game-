import {Component,type ErrorInfo,type ReactNode} from 'react';
import type {CrashRecorder} from './crashDiagnostics';
export class CrashBoundary extends Component<{recorder:CrashRecorder;children:ReactNode},{failed:boolean}>{state={failed:false};static getDerivedStateFromError(){return{failed:true}}componentDidCatch(error:Error,info:ErrorInfo){this.props.recorder.failure('unhandled-exception',new Error(`${error.stack??error.message}\nReact component stack:\n${info.componentStack}`))}render(){return this.state.failed?<main className="fatal-error"><h1>Die Oberfläche konnte nicht weiterlaufen.</h1><p>Dein letzter gültiger Spielstand bleibt erhalten.</p><button onClick={()=>this.props.recorder.download()}>Crash-Bericht exportieren</button><button onClick={()=>location.reload()}>Neu laden</button></main>:this.props.children}}
